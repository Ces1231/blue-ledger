# ── Secrets ──
.env
.env.local
.env.*.local
keys/
*.pem

# ── Go ──
blue-ledger-api/bin/
blue-ledger-api/*.exe
blue-ledger-api/tmp/

# ── Node / React ──
web/node_modules/
web/dist/
web/.vite/

# ── Editor ──
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Test / Coverage ──
coverage/
*.cover
*.out

# ── Docker ──
.docker/

# ── Terraform ──
terraform/.terraform/
terraform/**/.terraform/
terraform/**/*.tfstate
terraform/**/*.tfstate.backup
terraform/**/*.tfvars
!terraform/**/terraform.tfvars.example

# ── Claude agent files (not for git) ──
.claude/eitri/archive/

# JWT keys (never commit)
blue-ledger-api/keys/*.pem
*.pem
